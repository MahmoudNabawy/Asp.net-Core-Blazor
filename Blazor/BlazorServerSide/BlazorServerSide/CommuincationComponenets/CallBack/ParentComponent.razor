@page "/child"

<h3>ParentComponent</h3>

<p>Current count: @currentCount</p>

<ChildComponent SetCurrentCounter="SetCurrentValue"></ChildComponent>

@code {
    private int currentCount;

    private void SetCurrentValue(int value)
    {
        currentCount = value;
    }
}
